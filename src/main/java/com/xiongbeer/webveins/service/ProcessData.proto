option java_outer_classname = "ProcessDataProto";

message ProcessData {
    optional string urlFilePath = 1;

    enum Status {
        NULL = 0;
        WAITING = 1;
        RUNNING = 2;
        FINNISHED = 3;
        READY = 4;
    }

    required Status status = 2 [default=NULL];
}